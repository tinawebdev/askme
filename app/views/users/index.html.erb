<div class="container">
  <div>
    <h3>Наши пользователи</h3>
  </div>


  <% @users.each do |user| %>
    <div class="d-inline-flex p-2 ">
      <%= link_to image_tag(user_avatar(user), class: 'rounded'), user_path(user) %>
    </div>
  <% end %>
</div>

<div class="container mt-4">
  <div>
    <h3>Hashtags</h3>
    <% @hashtags.each do |h| %>
      <%= change_hashtags_to_links(h.name) %>
    <% end %>
  </div>
</div>
